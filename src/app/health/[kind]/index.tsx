import { MetricHistoryScreen } from '@/features/health/metric-history-screen'

/** History for one metric kind — `/health/[kind]`. */
export default function HealthMetricHistoryRoute() {
	return <MetricHistoryScreen />
}
