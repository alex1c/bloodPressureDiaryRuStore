/**
 * Current on-device schema version after all migrations have run.
 * Bump when adding a new migration module.
 */
export const CURRENT_SCHEMA_VERSION = 2

export const META_SCHEMA_VERSION_KEY = 'schemaVersion'
