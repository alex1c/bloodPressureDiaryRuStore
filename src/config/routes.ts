/**
 * Stable in-app routes used by Settings / «Ещё» navigation.
 * Expo Router maps `settings/index.tsx` → `/settings` (not `/settings/index`).
 */
export const SETTINGS_ROUTE = '/settings' as const

export const REMINDERS_ROUTE = '/settings/reminders' as const
