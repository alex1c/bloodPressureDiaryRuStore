# Copy this file to credentials/keystore.properties (gitignored) AFTER you
# generate the production keystore for BP Diary. Never commit real passwords.
#
# storeFile must be an absolute path to a keystore OUTSIDE the git repo.
# Example: C:/Users/YOU/secure/calculator-platform/bpdiary-release.jks
# Use forward slashes so Gradle accepts the path on Windows.

storeFile=C:/Users/YOU/secure/calculator-platform/bpdiary-release.jks
storePassword=REPLACE_ME
keyAlias=bpdiary
keyPassword=REPLACE_ME
